<UserControl x:Class="task.LoginControl"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:task"
             mc:Ignorable="d" 
             d:DesignHeight="170" d:DesignWidth="300">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>
        <Grid.ColumnDefinitions>
            <ColumnDefinition/>
            <ColumnDefinition Width="2*"/>
        </Grid.ColumnDefinitions>

        <TextBlock Grid.Row="0" Grid.Column="0" Text="Користувач:" Margin="10"/>
        <TextBox Grid.Row="0" Grid.Column="1" Name="UsernameTextBox" Margin="10"/>

        <TextBlock Grid.Row="1" Grid.Column="0" Text="Пароль:" Margin="10"/>
        <PasswordBox Grid.Row="1" Grid.Column="1" Name="PasswordTextBox" Margin="10"/>

        <Button Grid.Row="2" Grid.ColumnSpan="2" Content="Увійти" Margin="10" Click="LoginButton_Click" Name="LoginButton" IsDefault="True"/>

        <TextBlock Grid.Row="3" Grid.ColumnSpan="2" Name="ErrorMessageTextBlock" Margin="20" Visibility="Hidden" TextAlignment="Center" FontWeight="Bold"/>
    </Grid>
</UserControl>
